const baserate = 0;
const multiplier = 0.05e18;
const jumpmultiplier = 1.09e18;
const kink = 0.8e18;

module.exports = [baserate.toString(), multiplier.toString(), jumpmultiplier.toString(), kink.toString()];
